version: '3'

services:
  web: 
    build:
      context: .
      dockerfile: docker/dev/python/Dockerfile
    environment:
      PYTHONUNBUFFERED: 1
    volumes:
      - ./app:/app
    ports:
      - 8000:8000
    command: python manage.py runserver 0.0.0.0:8000
    